---
/**
 * Impressum — § 5 TMG compliant legal page, excluded from search engines.
 */
import SEOHead from "@webapp-factory/shared/components/SEOHead.astro";
import { getEntry } from "astro:content";
import Landing from "../layouts/Landing.astro";

const entry = await getEntry("legal", "impressum");
if (!entry) {
  throw new Error("Legal entry 'impressum' not found");
}
const { Content } = await entry.render();
const canonical = `${Astro.site?.toString() ?? "https://patchworkheroes.de"}impressum/`;
---

<Landing title={entry.data.title} description={entry.data.description} lang="de">
  <SEOHead
    slot="head"
    title={entry.data.title}
    description={entry.data.description ?? ""}
    canonical={canonical}
    noindex
  />
  <div class="prose mx-auto py-12">
    <Content />
  </div>
</Landing>
