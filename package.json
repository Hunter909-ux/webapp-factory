{
  "name": "webapp-factory",
  "private": true,
  "type": "module",
  "description": "Reusable web stack template — Astro frontend + CMS capability ports (Ghost default adapter)",
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm --recursive run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
